# A unique name for this app instance. This can be anything you like.
solalindenstein_energy_app:
  # 'module' points to the Python filename without the .py extension.
  module: solalindenstein_energy_manager
  # 'class' points to the class name inside that Python file.
  class: SolalindensteinEnergyManager